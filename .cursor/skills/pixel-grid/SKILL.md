---
name: pixel-grid
description: Legacy redirect for the older proof-only hotspot workflow. Use `background-hotspotting` instead for the full end-to-end background hotspot process.
---
# Pixel Grid

This skill has been superseded by `background-hotspotting`.

Use `background-hotspotting` when you need:
- grid proof generation
- minimal-label review proofs
- sprite matcher hints
- silhouette-based hotspot refinement
- manifest validation before approval

Keep this stub only so the old skill name still redirects to the unified workflow.
